[package]
name = "infrastructure"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
walkdir = "2"
domain = { path = "../domain" }
contracts = { path = "../contracts" }
